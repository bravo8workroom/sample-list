# 샘플 리스트 — 다음 단계 체크리스트

작성: 2026-07-23 / Supabase 공유 백엔드 전환 작업

## 지금 상태 (2026-07-23 세션 끝)
- ✅ 웹페이지(index.html) ↔ 백엔드 연결 코드 = **완료** (구문검증 통과, 아직 push 안 함)
- ✅ `schema.sql` **실행 성공** — products/submissions/photos/size_counts 재생성, settings·관리자로그인 보존 확인됨(REST 검증)
- ⚠️ **Storage 버킷 `sample-photos` = 아직 없음** — schema.sql의 `insert into storage.buckets`가 안 먹음(버킷 목록 빈 채로 확인됨). storage.objects **정책은 이미 깔림**, 버킷 알맹이만 없음.
- ⏳ **아직 GitHub에 push 안 함** → 지금 라이브 사이트는 예전 그대로 (안전)

## ▶ 다음 세션 첫 할 일 (여기부터)
1. **Storage 버킷 만들기** (UI로, 1분):
   Supabase → 왼쪽 **Storage** → **New bucket** → 이름 `sample-photos` → **Public bucket 토글 ON** → Create.
   (정책은 schema.sql로 이미 깔려서 버킷만 만들면 끝. 확인: 만든 뒤 목록에 sample-photos 보이면 OK)
2. 관리자 로그인 이메일/비번 확인 (없으면 Authentication→Users→Add user)
3. 아래 "실테스트" 진행 → 통과 시 push

---

## 사장님이 할 일

### ① schema.sql 실행 (선택 ① — 빈 테이블 새로 만들기)
> 관리자 로그인 계정·설정은 안 지워짐. 아직 실제 주문이 없어 안전.

1. 크롬에서 **supabase.com** 접속 → 지난번 계정으로 로그인
2. 프로젝트 클릭 (URL에 `zoszumqphwfniankrgio` 들어간 그 프로젝트)
3. 왼쪽 세로 메뉴에서 **SQL Editor** 클릭
4. 가운데 위 **+ New query** 클릭 (빈 입력창이 뜸)
5. 파일탐색기에서 `F:\projects\sample-list\schema.sql` **우클릭 → 연결 프로그램 → 메모장**
   → 메모장 열리면 **Ctrl+A**(전체선택) → **Ctrl+C**(복사)
6. Supabase 입력창 클릭 → **Ctrl+V**(붙여넣기)
7. 오른쪽 아래 **Run** 버튼 클릭 (또는 Ctrl+Enter)
8. 결과 확인:
   - 초록색 **Success** → 완료 ✅
   - 빨간 에러 → 그 글자 전체 복사해서 다음 세션에 붙여주기

### ② 관리자 로그인 정보 확인
- 지난번 만든 **이메일 + 비밀번호** 기억해 두기 (이제 로그인에 둘 다 필요)
- 모르면: Supabase → **Authentication → Users → Add user** 로 새로 만들기

---

## 다음 세션에서 할 일 (Claude가 진행)
1. schema.sql 잘 돌아갔는지 확인 (에러 있었으면 대응)
2. 브라우저 2개로 실테스트:
   - 관리자 창: 로그인 → 상품 추가
   - 시크릿 창(손님): 상품 보이는지 → 주문 → 관리자 창에 **실시간** 뜨는지
   - 손님 창: 사진 업로드 → 관리자 사진첩에 뜨는지
   - 재고 1개짜리를 두 창에서 동시에 → 한쪽만 성공(품절 차단) 확인
3. 통과하면 `F:\projects\sample-list`에서 **commit + push** → GitHub Pages 자동 재배포
4. 라이브 링크(https://rin870418-beep.github.io/sample-list/)에서 최종 확인

## 참고 (기술 메모)
- 접속키(공개용, 안전): URL `https://zoszumqphwfniankrgio.supabase.co`, publishable `sb_publishable_zRj6dXRMQ6RJ0hQygLnF1Q_dWy9T9M6`
- 데이터 구조: products=jsonb doc(정의만) / submissions=주문(관리자만) / size_counts=재고수(손님 공개) / photos=사진 / claim RPC=재고·한도 서버 검증
- 무해한 dead code 남음(나중 정리): idb 함수들, index.html 725행 adminPw
