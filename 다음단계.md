# 이어서하기 — 샘플 리스트

> **이 파일이 「지금 어디까지 했나」다.** 어느 컴퓨터·어느 AI로 열든 이 파일만 읽으면 바로 이어서 할 수 있다.
> 작업을 끝낼 때마다 아래 「지금 상태」와 「다음 할 일」을 고쳐서 같이 커밋한다.

마지막 갱신: 2026-09-12

## 한 줄 요약

라이브 운영 중. 기능은 다 들어갔고, 남은 건 **DB 마이그레이션이 실제로 적용됐는지 확인**과 잔손질뿐이다.

- 사이트: https://bravo8workroom.github.io/sample-list/ (GitHub Pages, `main` 브랜치가 곧 라이브)
- 백엔드: Supabase 프로젝트 `zoszumqphwfniankrgio`
- 파일: `index.html` 한 장이 전부(손님+관리자), `support.js`는 생성물이라 손대지 않는다

## 지금 상태 (최근 작업 순서)

| 날짜 | 커밋 | 내용 |
|---|---|---|
| 2026-09-11 | `d11ba4c` | 규칙을 `AGENTS.md` 한 곳으로 통일(모든 AI 공용) |
| 2026-08-20 | `dedda9e` | 사진 올리기 창에 누적 등록 장수 표시 + `photo-tally-migration.sql` 추가 |
| 2026-08-16 | `ee9c6ec` | 작업 저장 |
| 2026-08-02 | `c426f9f` | 완료 처리한 사람이 '사진 미제출'로 다시 잡히던 버그 수정 |
| 2026-08-02 | `6740bbc` | 저장소 남은용량 카드 + 완료 버튼(사진 삭제·개인정보 정리) + 사진 더보기 |

## 다음 할 일

1. **`photo-tally-migration.sql` 적용 확인** — 가장 최근 기능(`dedda9e`)이 이 SQL에 기대고 있다.
   확인법: Supabase → SQL Editor → `select photo_count('test','test');` 실행
   에러가 나면 `photo-tally-migration.sql`을 통째로 붙여넣고 Run 한 번.
2. 손님 창에서 사진 1장 올려보고 창 오른쪽 위 회색 글씨(`현재 N장 사진 등록완료`)가 늘어나는지 확인.
3. 관리자 '고객 사진 모음'에서 남은 용량 카드·완료 버튼이 정상인지 확인.
4. (여유 있을 때) 죽은 코드 정리 — `index.html`의 idb 관련 함수들은 이제 안 쓰인다.

## Supabase SQL 파일 안내 (전부 이미 만들어 둔 것들)

`schema.sql`만 기본이고 나머지는 **필요한 것만 한 번씩** Run 한다. ⚠ 이미 돌린 파일을 다시 돌릴 필요는 없다.

| 파일 | 무엇 |
|---|---|
| `schema.sql` | 기본 테이블·정책 (없는 것만 만든다 — 데이터 안 지워짐) |
| `buyers-migration.sql` | 초대제 + 아이디별 지갑식 수량관리 |
| `restore-allowlist-claim.sql` | 주문 가능 기준을 공개대상(#1)으로 되돌림 |
| `unclaim-migration.sql` | 관리자 명단 취소 기능 |
| `draft-photos-migration.sql` | 시안 사진(`kind='draft'`) 첨부 |
| `storage-lock-migration.sql` | 사진 보관함 비공개 — 관리자만 서명주소로 열람 |
| `storage-usage-and-done-migration.sql` | 남은 용량 표시 + 완료 처리(사진 삭제·개인정보 정리) |
| `photo-size-limit-migration.sql` | 사진 한 장 용량 한도 |
| `photo-tally-migration.sql` | 누적 등록 장수 장부 (**적용 확인 필요**) |

## 함정 메모

- `support.js`는 생성 파일이다. 직접 고치지 말 것.
- Supabase 접속키는 publishable(브라우저 노출용)이라 공개 저장소에 있어도 안전하다. **그 외 열쇠·개인정보는 절대 커밋 금지.**
- 무료 플랜이 7일 무요청이면 잠들어서 `.github/workflows/keepalive.yml`이 3일마다 깨운다. 건드리지 말 것.
